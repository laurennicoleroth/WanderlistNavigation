platform :ios, '12.1'

target 'Wanderlist' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Pring'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Mapbox-iOS-SDK'

  target 'WanderlistTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
